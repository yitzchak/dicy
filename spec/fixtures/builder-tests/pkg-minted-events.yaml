types:
  - command
  - log
events:
  - type: command
    rule: LaTeX(build;execute;job-1;pkg-minted.tex)
    command: >-
      pdflatex -file-line-error -interaction=batchmode -recorder -jobname=job-1
      -shell-escape pkg-minted.tex
  - type: command
    rule: LaTeX(build;execute;job-2;pkg-minted.tex)
    command: >-
      pdflatex -file-line-error -interaction=batchmode -recorder
      -output-directory=output -jobname=job-2 -shell-escape pkg-minted.tex
  - type: command
    rule: LaTeX(build;execute;job-1;pkg-minted.tex)
    command: >-
      pdflatex -file-line-error -interaction=batchmode -recorder -jobname=job-1
      -shell-escape pkg-minted.tex
  - type: command
    rule: LaTeX(build;execute;job-2;pkg-minted.tex)
    command: >-
      pdflatex -file-line-error -interaction=batchmode -recorder
      -output-directory=output -jobname=job-2 -shell-escape pkg-minted.tex
  - type: command
    rule: LaTeX(build;execute;job-1;pkg-minted.tex)
    command: >-
      pdflatex -file-line-error -interaction=batchmode -recorder -jobname=job-1
      -shell-escape pkg-minted.tex
  - type: command
    rule: LaTeX(build;execute;job-2;pkg-minted.tex)
    command: >-
      pdflatex -file-line-error -interaction=batchmode -recorder
      -output-directory=output -jobname=job-2 -shell-escape pkg-minted.tex
