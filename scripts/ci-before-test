#!/usr/bin/env bash

echo Updating package dependencies...
npm run update-dependencies

echo Bootstrapping...
if [[ "${APPVEYOR:-}" == true ]]; then
  npm run bootstrap -- --ignore=@dicy/cli
fi

npm run bootstrap
