types:
  - command
  - log
events:
  - type: command
    rule: LaTeX(build;execute;;pkg-epstopdf.tex)
    command: >-
      pdflatex -file-line-error -interaction=batchmode -recorder -shell-escape
      pkg-epstopdf.tex
  - type: command
    rule: LaTeX(build;execute;;pkg-epstopdf.tex)
    command: >-
      pdflatex -file-line-error -interaction=batchmode -recorder -shell-escape
      pkg-epstopdf.tex
