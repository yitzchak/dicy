%!TeX jobNames = job-1, job 2, job-3
% !TeX job-1:outputDirectory = output
% !TEX job 2 : shellEscape = enabled
%%!TeX $BIBINPUTS = foo, bar
%!tex bibtexEngine = mendex
%! TeX engine = baffle
%!TeX $PATH=wibble,
%!TEX syncTeX = yes
\documentclass[class=article]{standalone}

\usepackage{lipsum}

\begin{document}
\lipsum[2]
\end{document}
